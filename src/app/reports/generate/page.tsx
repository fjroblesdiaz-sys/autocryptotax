import { ReportGenerationContainer } from '@/features/reports/containers/report-generation.container';

/**
 * Report Generation Page
 * Shows progress while generating the report
 */
export default function GeneratePage() {
  return <ReportGenerationContainer />;
}

